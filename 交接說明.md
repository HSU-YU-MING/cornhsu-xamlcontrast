# 交接說明

> 2026-07-30。這份是給接手的人看的「現在在哪、下一步做什麼」。
> 完整設計見 `XamlContrast專案規畫書.md`。

## 現在有什麼

```
XamlContrast/
├── XamlContrast專案規畫書.md    ← 完整規劃（競品分析、架構、里程碑、風險）
├── 交接說明.md                  ← 這份
└── prototype/
    ├── XamlContrastTree.ps1     ← 可運作的原型，457 行
    ├── README.md                ← 怎麼跑、值得保留的坑
    └── baseline-*.txt           ← 三專案的實測輸出，移植後要重現
```

**還沒有**：repo、CI、測試、NuGet 專案檔、README。那些照 Parity 的骨架搬。

## 這東西為什麼值得做

2026-07-30 查證：

- 微軟官方的 Accessibility Insights for Windows **有**對比檢查，但是**手動的** ——
  懸停測一個元素，或用滴管取兩個像素。沒有全 App 掃描、沒有清單、沒有 CI。
- Rapid XAML 證明「XAML 靜態分析 + CI」這條路可行，但它的規則裡沒有對比度。
- GitHub 搜「xaml contrast accessibility」→ **0 個 repo**。
- NuGet / npm / GitHub 的 `XamlContrast` 精確名稱**全部未被使用**。

**空缺是真的，而且名字還沒人佔。**

## 原型的實戰成績

不是在 demo 上跑得動，是在**三個正式發行中的產品**上找到並修好 100+ 處真實問題：

| 專案 | 修正前（真實數字） | 修正後 |
|---|---|---|
| QuillNest | 破 13 / 偏低 31 | 破 2（皆假警報） |
| Kindling | 破 41 / 偏低 14 | 破 1（假警報） |
| CelFlow | 破 39 / 偏低 57 | 破 1（假警報）/ 偏低 1 |

促成的實際修正：CelFlow 572 處寫死色碼歸零、Kindling 48 處暗文字提亮、
QuillNest 26 處彩色按鈕底拆出專用色票。

三個專案得到同一個結論：**第三階暗文字（亮度 96）在深色底上不可能過 AA，
只該給停用態用** —— 這是工具量出來的，不是猜的。

> **進度更新（2026-07-31）**：**M0～M5 全部完成，v0.1.0 已發佈。**
> repo：https://github.com/HSU-YU-MING/cornhsu-xamlcontrast（CI 全綠）
> NuGet：`dotnet tool install -g Cornhsu.XamlContrast`（Trusted Publishing）
> 十二條解析規則、47 測試、兩套實作於四專案互證；四專案真實問題全數修畢
> （殘餘 fail 皆為已記錄假警報類，導入 CI 用 --write-baseline 承接）。
> 原型自此凍結（治理決定，見規劃書 M5 節）——新規則 C# 先行。
> 下一步見 ROADMAP：config 載入、TargetName 解析、npm 通路、M6 生態。
> 以下原文保留作歷史脈絡。

## 下一步：先 M0，再 M1

**M0（v0.2 新增）**：原型報告層有四個 bug（規劃書 8.2 節），其中兩個汙染基準線
（Style 配對全被靜默判成裝飾、「低於 AA」把裝飾算進去）。先修掉並重出四份
baseline —— 含目前缺的 **Cornea**。半天的事，但不做，後面全踩在錯的數字上。

**M1 色盤自動偵測 —— 擋住套件化的主閘。**

原型有三個寫死的色盤讀取器，每加一個專案就要改一次腳本 —— 這是套件化的反面。

要做的：
1. 掃專案裡所有 `ResourceDictionary`，收集 `<Color x:Key>` 與 `<SolidColorBrush x:Key>`
2. 推斷哪幾份是「同一組色票的不同主題版本」
   （鍵集重疊度高 ＋ 檔名/資料夾有 Dark/Light 線索）
3. 也要支援真相源在 C# 陣列的情況（Kindling 那種），但用可設定的樣式而非寫死
4. 偵測失敗要**明講**並退回「只算寫死色碼」模式，不可靜默退化

**驗收**：四個專案（Cornea / Kindling / QuillNest / CelFlow）零設定跑得出結果，
數字與 **M0 重出的** `baseline-*.txt`（四份）一致。
現有三份基準線有已知失真且缺 Cornea —— 見規劃書 M0 與 8.2 節。

## 接手時最該讀的三件事

1. **規劃書第 4.1 節** —— 難的不是 WCAG 公式（20 行），是「這段文字實際疊在什麼顏色上」
   的十七條解析規則。抄公式的實作會全部漏掉。
2. **規劃書第 4.4 節** —— 「對稱＝刻意」這個啟發法是錯的，它曾經吃掉 Kindling 52 組、
   CelFlow 96 組真實問題。等級與對稱是兩個獨立維度。
3. **prototype/README.md 的「值得保留的東西」表** —— 八個踩過的坑，
   每一個都是實測出來的，移植時請把註解一併帶走。

## 版本策略

**0.1.0 起，留在 0.x 直到介面凍結。** 照 Parity 的做法：
README 明寫「0.x 時 pin 到確切版本；1.0 後改用 @v1」。

1.0 條件見規劃書第 10 節。

## 有一件事要誠實記著

這支工具在開發過程中，**自己出過四次錯，而且每一次都是「靜默給出錯誤的信心」**：

1. 把「兩主題都低」判成刻意並藏起來 → 回報 Kindling「破 1」，實際 55 組低於 AA
2. 單一主題專案深淺同值 → 差距恆為 0 → 96 組全被吃掉
3. `ProgressBar` 的 `Foreground` 被當成文字 → 用 4.5 去要求一條進度條
4. PowerShell `.Count` 在單一結果時失準 → 「破 1 組」卻回報「全部達標」

**稽核工具最大的風險不是漏報，是謊報健康。**
任何新增的判斷邏輯（尤其是「這個可以放過」的邏輯），都要先問：
它會不會讓真實問題消失在報告裡？

（v0.2 補記：移植前的逐行 review 又抓到四個報告層問題 —— 見規劃書 8.2 節。
其中「Style 配對全被靜默判成裝飾」就是同一個形狀第五次出現。
回歸測試清單共八條，M2 要全部涵蓋。）

這是這個專案最該寫進 CONTRIBUTING 的一條。
